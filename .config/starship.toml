# Starship Configuration
# Vanta-Black Theme - Dark & Elegant

format = """
[╭─](bold white)\
$username\
$hostname\
$directory\
$git_branch\
$git_status\
$c\
$rust\
$golang\
$nodejs\
$php\
$java\
$kotlin\
$python\
$docker_context\
$line_break\
[╰─](bold white)$character"""

# Timeout for starship commands
command_timeout = 1000

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"

[username]
style_user = "bold bright-white"
style_root = "bold red"
format = "[$user]($style) "
disabled = false
show_always = true

[hostname]
ssh_only = false
format = "on [$hostname](bold bright-white) "
disabled = false

[directory]
truncation_length = 3
truncate_to_repo = true
format = "in [$path]($style)[$read_only]($read_only_style) "
style = "bold bright-cyan"
read_only = " 󰌾"
read_only_style = "red"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"Videos" = " "

[git_branch]
symbol = " "
format = "on [$symbol$branch]($style) "
style = "bold bright-magenta"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold red"
conflicted = "⚔️ "
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
untracked = "?${count}"
stashed = "📦"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "✘${count}"

[nodejs]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold green"

[python]
symbol = " "
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
style = "bold yellow"

[rust]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold red"

[golang]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold cyan"

[docker_context]
symbol = " "
format = "via [$symbol$context]($style) "
style = "bold blue"

[php]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold purple"

[java]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold red"

[c]
symbol = " "
format = "via [$symbol($version )]($style)"
style = "bold blue"

[package]
symbol = "📦 "
format = "is [$symbol$version]($style) "
style = "bold yellow"

[time]
disabled = false
format = '🕙[\[ $time \]]($style) '
time_format = "%T"
style = "bold white"

[cmd_duration]
min_time = 500
format = "took [$duration](bold yellow)"

[memory_usage]
disabled = false
threshold = 75
symbol = " "
format = "$symbol[${ram_pct}]($style) "
style = "bold dimmed white"
